import { Nav } from '../components/layout/Nav';

export function SettingsPage() {
  return (
    <div>
      <Nav />
      <div style={{ padding: 'var(--space-8)' }}>
        <h1>Settings</h1>
      </div>
    </div>
  );
}
